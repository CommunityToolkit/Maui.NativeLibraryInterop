<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiFirebaseMessagingSample.FIRMessagingPage">

   <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0">

            <Label
                Text="Firebase"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1"
                Padding="100,0" />

            <Label
                Text="Messaging Demo"
                Style="{StaticResource SubHeadline}"
                VerticalOptions="Center" />

            <Label
                Text="Client App FCM Token: "
                FontSize="18" />

            <Label
                x:Name="lblFcmToken"
                FontSize="18" />

            <Button
                x:Name="btnFcmRegister"
                Text="Register For Firebase Messaging"
                FontSize="18"
                Clicked="OnRegisterClicked"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
   </ScrollView>

</ContentPage>
